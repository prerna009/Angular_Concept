import { SmartModelFilterPipe } from './smart-model-filter.pipe';

describe('SmartModelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartModelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
