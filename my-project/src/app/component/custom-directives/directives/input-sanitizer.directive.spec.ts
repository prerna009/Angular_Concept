import { InputSanitizerDirective } from './input-sanitizer.directive';

describe('InputSanitizerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSanitizerDirective();
    expect(directive).toBeTruthy();
  });
});
